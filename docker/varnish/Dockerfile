ARG VARNISH_VERSION=7.6
ARG VARNISH_IMAGE_TAG=${VARNISH_VERSION}-alpine
FROM varnish:${VARNISH_IMAGE_TAG}
ARG VARNISH_VERSION

LABEL dev.govard.image.name="varnish"
LABEL dev.govard.varnish.version="${VARNISH_VERSION}"
